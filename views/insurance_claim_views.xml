<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- List View -->
    <record id="view_insurance_claim_tree" model="ir.ui.view">
        <field name="name">insurance.claim.list</field>
        <field name="model">insurance.claim</field>
        <field name="type">list</field>
        <field name="arch" type="xml">
            <list string="Insurance Claims" decoration-info="state == 'draft'" decoration-warning="state == 'submitted'" decoration-success="state == 'approved'" decoration-danger="state == 'rejected'">
                <field name="name"/>
                <field name="incident_id"/>
                <field name="insurance_type_id"/>
                <field name="claim_date"/>
                <field name="amount"/>
                <field name="state"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_insurance_claim_form" model="ir.ui.view">
        <field name="name">insurance.claim.form</field>
        <field name="model">insurance.claim</field>
        <field name="type">form</field>
        <field name="arch" type="xml">
            <form string="Insurance Claim">
                <header>
                    <button name="action_submit" 
                            string="Submit" 
                            type="object" 
                            class="oe_highlight" 
                            invisible="state != 'draft'"/>
                    <button name="action_approve" 
                            string="Approve" 
                            type="object" 
                            class="oe_highlight" 
                            invisible="state != 'submitted'" 
                            groups="insurance_module.group_insurance_manager"/>
                    <button name="action_reject" 
                            string="Reject" 
                            type="object" 
                            class="btn-danger" 
                            invisible="state != 'submitted'" 
                            groups="insurance_module.group_insurance_manager"/>
                    <button name="action_process" 
                            string="Process" 
                            type="object" 
                            class="oe_highlight" 
                            invisible="state != 'approved'" 
                            groups="insurance_module.group_insurance_manager"/>
                    <button name="action_reset_to_draft" 
                            string="Reset to Draft" 
                            type="object" 
                            invisible="state != 'rejected'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved,processed"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="incident_id"/>
                            <field name="insurance_type_id"/>
                            <field name="claim_date"/>
                            <field name="amount"/>
                        </group>
                        <group>
                            <field name="employee_id"/>
                            <field name="department_id"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                            <field name="currency_id"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description" placeholder="Add detailed description of the claim..."/>
                        </page>
                        <page string="Documents" name="documents">
                            <field name="document_ids" widget="many2many_binary"/>
                        </page>
                        <page string="Notes" name="notes">
                            <field name="notes"/>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- Search View -->
    <record id="view_insurance_claim_search" model="ir.ui.view">
        <field name="name">insurance.claim.search</field>
        <field name="model">insurance.claim</field>
        <field name="type">search</field>
        <field name="arch" type="xml">
            <search string="Search Insurance Claims">
                <field name="name"/>
                <field name="incident_id"/>
                <field name="insurance_type_id"/>
                <field name="employee_id"/>
                <field name="department_id"/>
                <separator/>
                <filter string="Draft" name="draft" domain="[('state','=','draft')]"/>
                <filter string="Submitted" name="submitted" domain="[('state','=','submitted')]"/>
                <filter string="Approved" name="approved" domain="[('state','=','approved')]"/>
                <filter string="Processed" name="processed" domain="[('state','=','processed')]"/>
                <filter string="Rejected" name="rejected" domain="[('state','=','rejected')]"/>
                <separator/>
                <filter string="My Claims" name="my_claims" domain="[('employee_id.user_id','=',uid)]"/>
                <group expand="0" string="Group By">
                    <filter string="Insurance Type" name="group_by_type" context="{'group_by':'insurance_type_id'}"/>
                    <filter string="Status" name="group_by_state" context="{'group_by':'state'}"/>
                    <filter string="Employee" name="group_by_employee" context="{'group_by':'employee_id'}"/>
                    <filter string="Department" name="group_by_department" context="{'group_by':'department_id'}"/>
                    <filter string="Claim Date" name="group_by_date" context="{'group_by':'claim_date'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_insurance_claim" model="ir.actions.act_window">
        <field name="name">Insurance Claims</field>
        <field name="res_model">insurance.claim</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_insurance_claim_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first insurance claim
            </p>
            <p>
                Create insurance claims and track their progress through the approval workflow.
            </p>
        </field>
    </record>
</odoo> 